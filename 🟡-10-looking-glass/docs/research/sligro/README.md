# Sligro — AsymX research layer

Prepare-only map. This file is not a verdict.

---

## 0. Purpose of this folder

This folder holds an **internal research and recovery** orientation around Sligro, logistics visibility topics, and how AsymX may **eventually** support **source-aware** questions — without turning that into a deliverable.

Material here may draw on:

- **public signals** (already separated in other repo paths where noted),
- **chat-derived** exploration (labeled where relevant),
- **repo-carried recovery** notes,
- **topic capsules** for sorting and navigation only.

This is **not** a public leaf, **not** an email, **not** a final report, and **not** a claim about internal Sligro facts. Hypotheses stay hypotheses until a **human gate** and **source checks** say otherwise.

---

## 1. Structure

| Layer | Path | Role |
| --- | --- | --- |
| Chat recovery index | [`chat_recovery/00_chat_recovery_index.md`](chat_recovery/00_chat_recovery_index.md) | Trace roster and pointers (prepare-only). |
| Signal table | [`chat_recovery/01_signal_extraction_table.md`](chat_recovery/01_signal_extraction_table.md) | Reusable signals with confidence and public-safety hints. |
| Duplicates / conflicts | [`chat_recovery/02_duplicates_conflicts.md`](chat_recovery/02_duplicates_conflicts.md) | Overlap and tension map between traces. |
| Core direction | [`chat_recovery/03_core_direction.md`](chat_recovery/03_core_direction.md) | Fold toward what stays visible vs parked. |
| Topic capsules | [`chat_recovery/topics/`](chat_recovery/topics/) (start at [`topics/00_topics_index.md`](chat_recovery/topics/00_topics_index.md)) | Topic sorting to **reduce repeated research loops** across AI chats. |

The **topics** layer exists so the same themes do not need to be re-derived from scratch each time a thread starts.

---

## 2. What this layer does

- Makes **existing** topics and recovery notes **easier to find**.
- Keeps **questions** and **hypotheses** labeled and **correction-friendly**.
- **Separates** what is **source-bound** in the repo from what is **chat-derived** or **NEEDS SOURCE CHECK**.
- **May help prepare** a future public-facing note **only after** an explicit **human gate** — nothing here assumes that gate has passed.

---

## 3. What this layer does NOT do

- It does **not** state or imply a known internal Sligro situation.
- It does **not** diagnose Sligro.
- It does **not** package a ready-made solution.
- It does **not** claim ROI, impact, or guaranteed outcomes.
- It does **not** speak for Sligro.
- It does **not** replace **source checks**, labels, or human review.

---

## 4. Current safe direction (Level 1 fold)

**One pattern. One period. One joint validation.**

A **possible** future step — only if invited and **human-gated** — would be to look at **one** limited, **source-bound** pattern (for example visibility questions around repeated **manco / short-pick / correction**-shaped events **per location** over **one agreed period**). The aim would **not** be to decide the outcome in advance, but to see whether a pattern is **jointly recognizable** and **worth validating** together. Anything without agreed data and scope stays an **open question** or **parked**.

*(Any specific counts or benchmarks are **NEEDS SOURCE CHECK** or out of scope until anchored.)*

---

## 5. Possible later routes

Options only — not scheduled actions:

- Keep this layer **internal**.
- Refine **topic capsules** as new repo leaves appear.
- Add a **public leaf** after **human gate** (not created here).
- Draft an **executive summary** only after **source checks** catch up.
- **Archive** material that remains too heavy for sharing.

---

## 6. Signature direction

If a signature is needed:

AsymX — A field 2 look.  
Created by looking.
