# Workbench

**Status:** experimental workspace  
**Function:** local and early Level 1 lenses that are useful, but not the main public entrance.

The workbench contains prototypes, intake experiments and early filts.

Current areas:

- `chat-intake-level1/`
- `story-filter-level1/`

Rule:

Workbench material may be useful, but it should not define the public identity of AsymX by itself.
